---
description: 逆向工程专用规则，进行逆向分析时由AI判断引入
alwaysApply: false
---

# 逆向工程规则

- 算法逆向优先，不依赖浏览器自动化
- 用户要求"不能用自动化"时，必须提供纯算法实现
- 每个API请求输出完整JSON响应（HTTP状态码、Content-Type、响应长度）
- 数据自动保存到文件
- 专注算法逆向，不主动添加自动化方案
- 优先纯 Python 算法实现，或其它算法方式（如扣代码、Node/execjs 调用等）；使用或建议浏览器自动化前必须征得用户同意
- 不自动生成 README/总结/分析类 Markdown 文档，除非用户明确要求
- 输出不用 emoji，用 [OK]/[FAILED]/[WARNING] 等纯文本
- 程序界面与回复使用中文
- 从 JS 扣出的代码若需在旧环境运行，使用 ES5（var、function，无箭头函数、let/const、模板字符串）
- 多 agent 并行改代码前先做规划再执行
