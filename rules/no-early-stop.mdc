---
description: 强制：问题没解决就别停。不得以「已给建议」「已更新文档」「可选方案」作为结束；必须持续执行直到目标达成或仅剩一条必须由用户执行的操作。
alwaysApply: true
---

# 禁止提前停止（全局）

0. **强制：没解决就别停**
   - 用户提出的问题/目标未达成时，**禁止**以「已提供方案」「已更新 NOTE」「可选方案如下」「建议你…」等作为回复结尾并结束。
   - 结束条件只有两个：**目标已达成**，或**确实只剩一条必须用户在本机执行的操作**（且已明确写出该一条）。除此之外必须继续执行、换思路、重试、诊断。

1. **不得在未解决问题时主动停止**
   - 不要给出方案、文档或「可选步骤」后就结束回复。
   - 不要停在「请先配置」「请确保环境」「可以尝试」等前置条件上；先执行，失败再根据输出继续。

2. **必须持续执行与测试**
   - 能跑的命令就跑，能点的就点，能重试的就重试（含换参数、换顺序、换工具）。
   - 失败时执行有针对性的诊断（查版本、进程、文件、端口、日志），根据结果决定下一步；若当前思路走不通，换另一种可行思路继续，而不是把问题总结成「可选方案」后停。

3. **允许暂停的唯一情况**
   - 仅当**确实需要用户在本机执行一条具体操作**（如打开某软件、放入某文件、在弹窗点允许）时，才暂停并**明确写出这一条**；用户完成后继续从断点推进，不反复暂停。

4. **适用场景**
   - 所有任务，尤其逆向、过检测、连接设备、跑脚本、调试等：以「问题解决」或「只剩一条用户操作」为结束条件，**不以「已给建议」「已列可选方案」为结束条件**。
